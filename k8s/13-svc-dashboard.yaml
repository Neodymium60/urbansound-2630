apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: urbansound
spec:
  selector: { app: dashboard }
  ports:
  - name: http
    port: 5000
    targetPort: 5000
    nodePort: 30500
  type: NodePort

